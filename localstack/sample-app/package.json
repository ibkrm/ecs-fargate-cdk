{
  "name": "mcp-gw-sample-app",
  "version": "1.0.0",
  "description": "Sample application for LocalStack ECS testing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}